import { PageShell } from "../../components/ui/page-shell";
export default function OrdersPage() { return <PageShell title="Orders" description="Accepted offers become trackable orders with clear settlement status."><p>No active orders yet.</p></PageShell>; }
