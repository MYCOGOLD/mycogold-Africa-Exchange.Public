import { PageShell } from "../../../components/ui/page-shell";
export default function DataPipelinePage() { return <PageShell title="Data pipeline" description="Observe ingestion, validation, aggregation, and index publication stages."><p>Pipeline status: awaiting production event infrastructure.</p></PageShell>; }
