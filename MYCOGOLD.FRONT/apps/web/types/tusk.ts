export type TuskListingDraft = { product?: string; quantity?: number; unit?: string; location?: string; price?: number; currency?: string; availableDate?: string };
export type TuskSignal = { id: string; title: string; detail: string; confidence: number };
